<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.tliaswebmanagement.Mapper.LogMapper">

    <insert id="insertLogData" useGeneratedKeys="true" keyProperty="id">
        insert into operate_log(operate_emp_id, operate_time, class_name, method_name, method_params, return_value, cost_time)
            values
                (#{operateEmpId},#{operateTime},#{className},#{methodName},#{methodParams},#{returnValue},#{costTime})
    </insert>

    <select id="queryLogData" resultType="org.example.tliaswebmanagement.EntityClass.OperateLog">
        select operate_log.*,employee.name
        from
            operate_log
                inner join
            employee
            on
                operate_log.operate_emp_id = employee.id
    </select>

</mapper>